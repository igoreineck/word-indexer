#include "libs/indexer.h"
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    readFile();
}


// BACKUP FOR USE LIST
// Lista lista;
// inicializaLista(&lista, sizeof(float));
// float x = 8.9, y;
// insereNoInicio(&lista, &x);
// mostraLista(lista, mostra_float);
// x = 19.75;
// insereNoInicio(&lista, &x);
// mostraLista(lista, mostra_float);
// y = 25;
// insereNoFim(&lista, &y);
// mostraLista(lista, mostra_float);
// mostraLista(lista, mostra_float);